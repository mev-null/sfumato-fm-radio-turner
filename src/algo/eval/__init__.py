"""
algo.eval — algo の品質を定量化するメトリクス層。

ここに置く関数は「アクセプタンス・オラクル」として使う:
- algo(Python)の復調出力を測ってゲートにする(1.7)
- 純粋関数で実装に依存しないので、将来 HW に移植する場合もシミュレーション出力を
  同じ関数に通し、モデル↔実装を apples-to-apples で比較できる

純粋関数(入力 → 数値)に保つこと。乱数・I/O・グローバル状態を持ち込まない。
"""
