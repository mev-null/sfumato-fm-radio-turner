# ADR-006: [algo] 復調純度(THD/SINAD)の改善 — モノ測定・パイロット除去・イメージ除去とフィルタ方式

- ステータス: Accepted
- 日付: 2026-06-07
- 領域: algo
- 関連: [../roadmap.md](../roadmap.md)(1.7 / デシメーション多段化)/ [adr-005-demod-quality-gate.md](adr-005-demod-quality-gate.md) / [adr-007-stereo-separation.md](adr-007-stereo-separation.md) / [adr-001(保留)](../future/adr/adr-001-rf-frontend-low-if-adc.md)

## コンテキスト

復調品質ゲート(ADR-005)で品質を定量化したところ、初期実装は **THD 1.45% / SINAD 32dB**(目標 Sony THD ≤0.3% / SINAD ≥75dB)と低かった。測定で雑音・歪みの正体を切り分けた結果、原因は単一ではなく複数あった:

1. **測定基準の汚染**: THD/SINAD をステレオ復調後の L チャンネルで測っており、サブキャリア検波経路の残差が混入していた(THD が実態より大きく見えていた)。
2. **19kHz パイロット漏れ**: 音声帯域制限が Butterworth 5次(IIR)で、なだらかな遮断のため 19kHz パイロットを約 11dB しか落とせず、雑音電力の **86%** を占めていた。
3. **複素ミキサのイメージ折返し**: 実信号を複素 LO で混ぜると 2·fc(=500kHz)に像が出るが、`np.angle` の前に除去していなかったため位相が汚れ、SINAD を ~50dB に張り付かせていた(SNR 非依存=決定論的な床)。

これらの根は**フィルタの選び方・位相特性**にある。受信チェーンのフィルタ方式と、純度(THD/SINAD)の作り込み方を定める必要がある。フィルタ方式の候補は 全 IIR / 全 FIR / 使い分け(位相律速段だけ FIR)で、将来のハードウェア移植を妨げないことも考慮し、位相忠実性が要る段に限って FIR を使う方針に収束した。

## 決定

純度を次の 4 点で作り込む。THD は 1.45→**0.0072%**(ハードゲート昇格)、SINAD は 32→**66.8dB** に改善した。

- **(1) THD/SINAD はモノラル復調経路で測る**: ステレオ・マトリクスを通さない `_mono_decode`(main=L+R)で測り、FM 復調チェーン単体の純度を評価する。Sony の SINAD(S/N)75dB は元々**モノ規格**で整合的。これで THD の実態が 0.097% と判明し Sony 絶対ゲートへ昇格(baseline は schema v2、ADR-005 追記参照)。
- **(2) 19kHz パイロット除去 = 15kHz ポリフェーズ間引き FIR**: 音声帯域制限+間引き(192k→48k)を等リプル FIR(`remez`、15k 通過 / 18k 阻止)で行い、`upfirdn` のポリフェーズ間引きで適用(`dsp/filters.py:design_audio_decimation_fir`)。SINAD 40.9→49.8dB。
- **(3) チャネル選択(イメージ除去)LPF**: 複素ミキシング後・判別器前に Butterworth LPF を入れ、2·fc=500kHz の像を除去してから位相を取る(`receiver._channel_select`、250kHz/6次)。これが**最大レバーで SINAD 49.8→66.8dB(+17dB)、THD も 0.096→0.0072%**。HW では DDC のチャネル選択フィルタに相当。
- **(4) フィルタ方式 = 位相律速段は線形位相 FIR、非依存段は IIR(適材適所)**: 音声帯域制限・ステレオ分割(ADR-007)は線形位相 FIR、de-emphasis と高レートのチャネル選択は IIR(低次)で軽く。受信機は素のストリーミング出力を返し、定常区間の切り出しは測定側(eval/harness)の責務とする。

**スコープ外(今回やらない)**: SINAD の残り 8.2dB(66.8→75dB)。バイパス実験でアーキの素の床(~67dB)に到達済みで、ここから先は TX 側イメージ(192k→2.3M アップサンプル)や測定床に踏み込む逓減領域。高レート段(2.3M)の FIR 化も別途(CIC/多段へ再設計)。

### 将来の移植を見据えた理由 — なぜ位相律速段だけ FIR か

- FPGA で希少なのは**乗算器(DSP ブロック)**。音声・MPX の低レート処理はクロック(数十 MHz)に対し桁違いに遅く、**乗算器1個を時分割**して長い FIR を回せる → 低レートでは FIR の多タップは実質安価で、線形位相・常時安定・固定小数点耐性の利点だけが残る。
- 高レート(RF 2.3M)はサイクル余裕が小さく鋭い FIR が高コスト → 低次 IIR か、定石の CIC(乗算器ゼロの間引き)/多段が有利。チャネル選択を低次 IIR にしたのはこのため。
- 高次 IIR は極が単位円付近で固定小数点に弱い(リミットサイクル・不安定化)。位相律速段を FIR にすることは将来の固定小数点化([../future/roadmap-hardware.md](../future/roadmap-hardware.md) 2.3)とも整合する。

## 影響

### 良い影響
- THD が製品水準(0.0072%)に達しハードゲート昇格。SINAD は 32→66.8dB と +35dB 改善。雑音の主因(パイロット・イメージ)を測定根拠で除去できた。
- モノラル測定が「復調チェーン単体の純度」と「ステレオ・マトリクスの質(ADR-007)」を切り分ける基準線になる。
- 線形位相 FIR は固定小数点に強く、係数 ROM + 遅延ラインで RTL に素直に落ちる。

### 受け入れるトレードオフ / 負の影響
- FIR は群遅延が大・タップ多(低レートでは乗算器時分割で吸収)。受信機が定常でない出力を返すため、評価側で測定窓の切り出しが必要(責務分離の代償)。
- イメージ除去の IIR(2.3M)は algo モデルで、HW 最終形(CIC/多段)とは別。

### 将来への含み
- SINAD 75dB へは TX イメージ・測定床の改善(逓減領域)。
- 2.3M フロントエンドは CIC ベース多段へ再設計(roadmap「デシメーション多段化」)。

## 備考
- 実装: `src/algo/dsp/filters.py`(FIR 設計)/ `src/algo/radio/receiver.py`(`_mono_decode` / `_channel_select`)/ `src/algo/eval/harness.py`(モノ測定・測定窓)/ `src/algo/settings.py`(定数集約、Rule 3)。
- ゲート機構は [adr-005-demod-quality-gate.md](adr-005-demod-quality-gate.md)、ステレオ分離は [adr-007-stereo-separation.md](adr-007-stereo-separation.md)。
